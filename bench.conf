# bench.conf
# Single source of truth for lightweight bench defaults.
# Edit this file to change the default iteration count used by the
# bash driver and the Python wrapper. Keep it small and shell-compatible.

DEFAULT_ITERATIONS=1
